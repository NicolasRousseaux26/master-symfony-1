<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="{{ path('homepage') }}">Master</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbar">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('homepage') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('product_list') }}">Les produits</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('product_create') }}">Ajouter un produit</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('category_index') }}">Les catégories</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <div class="dropdown">
                        <button class="btn btn-secondary dropdown-toggle" type="button" data-toggle="dropdown">
                            {{ app.user.username }}
                        </button>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('logout') }}">Se déconnecter</a>
                        </div>
                    </div>
                {% else %}
                    <li class="nav-item">
                        <a class="btn btn-primary" href="{{ path('login') }}">Se connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div> <!-- .container -->
</nav>
